import { DataTypes } from '../utils/Utilities';
export default{
    computed: {
        cType: 'ContentComponent',
        dataTypes () {
            return DataTypes;
        },
        propsMap () {
            return [];
        },
    },
};